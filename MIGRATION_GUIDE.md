# サイト移行 作業手順書（依頼主さま向け）
## 「住まいの設備屋さん」ホームページ ― Wix → Cloudflare Pages + Decap CMS への移行

このドキュメントは、ホームページの引っ越し作業のうち「依頼主さまご自身で
行っていただく必要がある作業」だけをまとめたものです。上から順番に、
1つずつチェックしながら進めていただければ完了します。

> **わからない箇所があれば、番号（例:「手順4がわかりません」）を伝えていただければ、
> 一緒に画面を確認しながら進めます。** 無理に一人で完璧にやろうとしなくて大丈夫です。

### 全体の所要時間の目安
すべて合計するとおおよそ **2〜4時間**（待ち時間・確認時間を含む）です。
一度に全部やる必要はありません。区切りの良いところまで進めたら、
続きは別の日でも問題ありません。

### 事前にお読みください（重要）
- **手順9（独自ドメインの設定変更）だけは、サイトが一時的に表示できなくなる
  リスクがあります。** この手順だけは、必ず事前にClaude Codeと「いつ実行するか」
  のタイミングを確認してから行ってください。他の手順は現在稼働中のWixサイトに
  影響しませんので、安心して進めていただけます。
- 依頼主さまのメールアドレスやパスワードなどの個人情報は、このファイルや
  リポジトリには書き込まないでください。アカウント作成時にご自身が普段お使いの
  メールアドレスをご入力いただければ問題ありません。

---

## 手順1: GitHubアカウントを作成する

**かかる時間の目安:** 5分

GitHubは、ホームページの設計図（ファイル一式）を保管しておく場所です。
今後、施工実績の写真を追加する作業もここを経由して行われます。

1. https://github.com/ にアクセスする
2. 「Sign up（新規登録）」からアカウントを作成する
   - ユーザー名・メールアドレス・パスワードを入力
   - メール認証を求められたら、届いたメールのリンクをクリックする
3. 無料プラン（Free）のままで問題ありません。有料プランへの案内が出ても
   スキップして構いません。

**すでにGitHubアカウントをお持ちの場合:** この手順はスキップして、
そのアカウントを手順2以降で使ってください。

**つまずきやすいポイント:** ユーザー名は後から変更すると各所のURLがずれて
面倒なので、一度決めたらそのまま使うのがおすすめです（会社名やローマ字など、
覚えやすいものでOKです）。

---

## 手順2: GitHubで新しいリポジトリ（保管場所）を作成する

**かかる時間の目安:** 5分

「リポジトリ」とは、ホームページのファイル一式を入れておくフォルダのような
ものだと考えてください。

1. GitHubにログインした状態で、右上の「+」→「New repository」をクリック
2. 「Repository name（リポジトリ名）」を入力する
   - 例: `sumainosetsubiya-hp`
3. 「Public」と「Private」のどちらかを選ぶ
   - **Public（公開）**: 誰でもファイルの中身を見られる。無料プランでも制限なし。
   - **Private（非公開）**: GitHubアカウントを持つ人のうち、招待した人しか
     ファイルの中身を見られない。
   - どちらでも今回の仕組みは問題なく動作します。「社外の人にソースコードを
     見られたくない」というお考えがあれば Private を、特にこだわりがなければ
     Public のままで構いません。迷ったら Private を選んでおくのが無難です。
4. 「Add a README file」などのチェックボックスは **チェックを入れずに**
   そのまま「Create repository」をクリックする
5. 作成できたら、表示されているURL（例: `https://github.com/ユーザー名/リポジトリ名`）
   を控えておいてください。手順3・8で使います。

**つまずきやすいポイント:** 「Add a README file」にチェックを入れてしまうと、
後でファイルをアップロードする際に一手間増えるだけなので、チェックを入れて
しまっても大きな問題にはなりません。心配な場合はチェックを外したままにしてください。

---

## 手順3: 今お使いのフォルダの中身をGitHubにアップロード（push）する

**かかる時間の目安:** 5〜15分（多くの場合、実際の操作はClaude Codeが代行できます）

このパソコンにある「住まいの設備屋さんHP制作」フォルダの中身一式を、
手順2で作ったGitHubのリポジトリに送る作業です。

**このコマンド操作自体はClaude Codeが代行できますので、
「手順3をお願いします、リポジトリのURLは〇〇です」とお伝えいただければ大丈夫です。**
ご自身で操作されたい場合は、以下の手順で進められます。

1. Gitがインストールされているか確認する（ターミナル／コマンドプロンプトで）
   ```
   git --version
   ```
   バージョン番号が表示されればインストール済みです。表示されない場合は
   https://git-scm.com/downloads からインストールしてください。
2. プロジェクトフォルダの中で、以下を順番に実行する
   ```
   git init
   git add .
   git commit -m "初回コミット"
   git branch -M main
   git remote add origin https://github.com/ユーザー名/リポジトリ名.git
   git push -u origin main
   ```
3. `git push` の際にGitHubのユーザー名とパスワードを聞かれることがあります。
   2021年以降、GitHubは通常のパスワードでの認証を廃止しているため、
   その場合はブラウザでの認証、または「Personal Access Token」の入力を
   求められます（これもClaude Codeと一緒であれば対応できます）。

**つまずきやすいポイント:** 認証まわりでうまくいかないことが最も多いポイントです。
うまくいかない場合は、エラーメッセージのスクリーンショットを見せていただければ
一緒に解決できますので、無理に自己解決しようとせず早めに聞いてください。

---

## 手順4: GitHubで「OAuth App」を新規登録する

**かかる時間の目安:** 10分

これは、Decap CMS（施工実績を追加編集する管理画面）が「このGitHubアカウントの
持ち主が操作しています」と確認するための仕組みです。

1. GitHubの右上のアイコン → 「Settings」をクリック
2. 左メニューの一番下の方にある「Developer settings（開発者向け設定）」をクリック
3. 「OAuth Apps」→「New OAuth App」をクリック
4. 以下のように入力する
   - **Application name（アプリ名）:** 例:「住まいの設備屋さん CMS」
   - **Homepage URL:** 今の時点でわかる範囲でOKです。独自ドメインが決まって
     いなければ、手順6で発行されるCloudflare PagesのURL
     （`https://（プロジェクト名）.pages.dev` の形式）を、後で戻って
     入力し直しても構いません。仮に `https://sumainosetsubiya.com` と
     入力しておいても問題ありません。
   - **Authorization callback URL:** ここが一番重要です。手順7で
     Cloudflare Workerをデプロイすると、`https://（Worker名）.（サブドメイン）.workers.dev`
     のようなURLが発行されます。その **末尾に `/callback` を付けたもの**
     （例: `https://decap-cms-oauth-proxy.abcd1234.workers.dev/callback`）
     を入力します。
     **今の時点ではまだこのURLは分かりません。** 一旦仮の値
     （例: `https://example.com/callback`）を入れて登録し、
     手順7が終わってWorkerのURLが確定した後に、このページに戻ってきて
     正しい値に書き換えてください（このOAuth Appの設定画面はいつでも
     編集し直せます）。
5. 「Register application」をクリックする
6. 登録が終わると「Client ID」が表示されます。控えておいてください。
7. 「Generate a new client secret」をクリックし、表示された
   「Client Secret」を控えておいてください。
   **この画面を離れると二度と表示されなくなるので、必ずこの時点でメモしてください**
   （メモ帳などに貼り付けて保存し、他人と共有しないでください）。

**つまずきやすいポイント:** Client Secretは表示後にコピーし忘れると
再確認できず、その場合は「Generate a new client secret」をもう一度
押して再発行すれば大丈夫です（古いSecretは無効になります）。

---

## 手順5: Cloudflareアカウントを作成する

**かかる時間の目安:** 5分

1. https://dash.cloudflare.com/sign-up にアクセスする
2. メールアドレスとパスワードを入力してアカウントを作成する
3. 届いた確認メールのリンクをクリックして認証を完了する
4. 無料プラン（Free）のままで、今回の用途はすべて対応できます。

**つまずきやすいポイント:** 特にありません。GitHubと同様の一般的な
アカウント作成です。

---

## 手順6: Cloudflare Pagesで新規プロジェクトを作成し、GitHubリポジトリと連携する

**かかる時間の目安:** 10〜15分

ここで、実際にホームページがインターネット上に公開されます。

1. Cloudflareダッシュボードにログインする
2. 左メニューから「Workers & Pages」を選ぶ
3. 「Create（作成）」→「Pages」タブ →
   「Connect to Git（Gitに接続）」を選ぶ
4. GitHubアカウントとの連携を求められるので許可する
   （初回のみ、どのリポジトリへのアクセスを許可するか選択する画面が出ます。
   手順2で作ったリポジトリを選択してください）
5. 対象のリポジトリ（例: `sumainosetsubiya-hp`）を選んで「Begin setup」
6. ビルド設定の画面が出ますが、このサイトは元々完成済みのHTML/CSS/JS
   ファイルなので、特別なビルド作業は不要です。以下のように設定してください。
   - **Framework preset（フレームワーク）:** 「None」のまま
   - **Build command（ビルドコマンド）:** 空欄のままでOK（何も入力しない）
   - **Build output directory（公開ディレクトリ）:** `/` のまま
     （すでに入っている場合はそのまま変更不要です）
7. 「Save and Deploy」をクリックする
8. 数分待つとデプロイが完了し、`https://（プロジェクト名）.pages.dev`
   というURLでサイトが公開されます。このURLを開いて、実際に
   ホームページが表示されることを確認してください。

**つまずきやすいポイント:** ビルドコマンドの欄に何か入力してしまうと
エラーになることがあります。空欄のままで問題ありませんので、
余計な文字が入っていないか確認してください。

---

## 手順7: Cloudflare WorkerでOAuthプロキシをデプロイし、環境変数を設定する

**かかる時間の目安:** 15〜20分

手順4で作ったOAuth Appの「鍵」を安全に扱うための中継サーバーを、
Cloudflare Workers上に立てます。このリポジトリの
`cloudflare-worker-oauth/` フォルダに、そのためのコードと
詳しい手順（`cloudflare-worker-oauth/README.md`）を用意しています。

1. `cloudflare-worker-oauth/README.md` を開き、「方法A（wrangler CLI）」
   または「方法B（ダッシュボードで直接貼り付け）」のどちらかで進める
   （IT操作に慣れていなければ方法Bがおすすめです）
2. デプロイが終わると、Workerの公開URL
   （例: `https://decap-cms-oauth-proxy.abcd1234.workers.dev`）が
   発行されるので控えておく
3. そのWorkerの管理画面の「Settings」→「Variables and Secrets」から、
   手順4で控えた以下の2つを **Secret（暗号化）** として登録する
   - `GITHUB_CLIENT_ID`
   - `GITHUB_CLIENT_SECRET`
4. 手順4に戻り、GitHub OAuth Appの「Authorization callback URL」を、
   ここで発行されたWorker URLの末尾に `/callback` を付けたものに
   書き換えて保存する
   （例: `https://decap-cms-oauth-proxy.abcd1234.workers.dev/callback`）

**この手順で控える情報（次の手順8で必要です）:**
- Workerの公開URL

**つまずきやすいポイント:** 「手順4のcallback URL」と
「手順7のWorker URL + /callback」が1文字でも食い違うと、
ログインボタンを押したときにGitHubがエラー画面を出します。
コピー＆ペーストで正確に一致させるのがポイントです。

---

## 手順8: `admin/config.yml` のリポジトリ名を書き換える

**かかる時間の目安:** 5分（Claude Codeが代行できます）

このリポジトリの `admin/config.yml` というファイルには、
現時点では仮の値（プレースホルダー）が入っています。
これを、手順2・7で確定した実際の値に書き換える必要があります。

**この書き換え作業はClaude Codeが代行できます。**
以下の2つの情報をClaude Codeに伝えていただくだけで完了します。

- GitHubの「ユーザー名/リポジトリ名」（例: `yamada-taro/sumainosetsubiya-hp`）
- 手順7で控えたCloudflare WorkerのURL
  （例: `https://decap-cms-oauth-proxy.abcd1234.workers.dev`）

ご自身で書き換えたい場合は、`admin/config.yml` を開き、
`backend.repo` と `backend.base_url` の2箇所（コメントに
「書き換えてください」と書いてある行）を実際の値に直し、
GitHubへ保存（コミット・push）してください。

**つまずきやすいポイント:** このファイルを直接GitHub上の
「ファイルを編集」画面から書き換えることも可能です
（右上の鉛筆アイコン→ 編集 → 一番下の「Commit changes」）。
パソコン側と手元が二重管理にならないよう、どちらか一方の方法に
決めて作業してください（迷ったらClaude Codeに任せるのが確実です）。

---

## 手順9: 独自ドメイン（sumainosetsubiya.com）をCloudflareに追加する

**かかる時間の目安:** 30分〜数時間（ドメインの反映待ち時間を含む。実作業は15分程度）

> **重要:** この手順は、設定内容やタイミングを誤ると、
> ドメイン（sumainosetsubiya.com）でサイトが一時的に表示されなくなる
> 可能性があります。**必ず事前にClaude Codeと「いつ実行するか」を
> 相談してから着手してください。** 夜間や来店予約の少ない時間帯に
> 行うと安心です。

1. まず、現在のドメイン（sumainosetsubiya.com）が
   **どこで管理されているか** を確認してください。
   - Wixの管理画面にログインし、「ドメイン」の設定メニューを開く
   - そこに「このドメインはWixで購入・管理されています」といった
     表示があれば、登録先（レジストラ）はWixです
   - 逆に「外部で管理されているドメインを接続しています」といった
     表示がある場合は、お名前.com・ムームードメインなど、
     Wix以外の別会社で管理されている可能性があります
   - どちらか分からない場合は、Wixの画面をスクリーンショットで
     見せていただければ、一緒に確認します
2. Cloudflareダッシュボードで「Add a Site（サイトを追加）」から
   `sumainosetsubiya.com` を追加する
3. Cloudflareが「ネームサーバー（例: ○○.ns.cloudflare.com）」を
   2つ発行するので、これを控える
4. 手順1で確認した登録先（WixまたはWix以外のレジストラ）の管理画面で、
   ドメインの「ネームサーバー」設定を、Cloudflareが発行したものに
   書き換える
   - Wixで管理している場合: Wixの「ドメイン設定」→
     「ネームサーバーを使用する」に類するメニューから変更
   - 外部レジストラの場合: そのレジストラの管理画面にログインし、
     同様にネームサーバーを変更
5. 反映されるまで数十分〜最大48時間ほどかかることがあります
   （実際にはもっと早く切り替わることが多いです）
6. Cloudflareダッシュボードでドメインが「Active（有効）」になったら、
   手順6で作ったCloudflare Pagesプロジェクトの
   「Custom domains（カスタムドメイン）」から
   `sumainosetsubiya.com` を追加する

**つまずきやすいポイント:** ネームサーバーの切り替え中は、
一時的にメールの送受信やWix側の他の機能（もしあれば）に
影響が出ることがあります。切り替え作業に着手する前に、
必ずClaude Codeとタイミングを確認してください。

---

## 手順10: Formspree（お問い合わせフォーム）のアカウント作成とフォームID取得

**かかる時間の目安:** 10分

お問い合わせページ（`contact.html`）のフォームは、Formspreeという
外部サービスを使って送信内容をメールで受け取る仕組みになっています。
この手順は以前にもご案内した内容ですが、今回の一連の作業と合わせて
行うと効率的なため、改めて記載します。

1. https://formspree.io/ にアクセスし、アカウントを作成する
   （無料プランで開始できます）
2. ログイン後、「New Form（新しいフォームを作成）」を選ぶ
3. フォームの名前を適当に入力する（例: 「お問い合わせフォーム」）
4. 作成すると、`https://formspree.io/f/xxxxxxxx` のような
   「フォームID付きのURL」が発行されるので、これを控える
5. このURL（フォームID）をClaude Codeに伝えていただければ、
   `contact.html` 内の `YOUR_FORM_ID` の部分を実際の値に書き換えます
6. 通知を受け取りたいメールアドレスをFormspreeの管理画面で設定する
   （フォーム作成時に使ったメールアドレスが初期設定で使われます）

**つまずきやすいポイント:** 無料プランは月間の送信件数に上限があります
（目安として月50件程度）。問い合わせが増えてきた場合は、有料プランへの
アップグレードをご検討ください。

---

## すべて完了したら

以下がすべて終わっていれば、移行作業は完了です。

- [ ] 手順1: GitHubアカウント作成
- [ ] 手順2: GitHubリポジトリ作成
- [ ] 手順3: ファイル一式をGitHubへpush
- [ ] 手順4: GitHub OAuth App登録（Client ID / Secret取得）
- [ ] 手順5: Cloudflareアカウント作成
- [ ] 手順6: Cloudflare PagesでGitHub連携・公開
- [ ] 手順7: Cloudflare WorkerでOAuthプロキシをデプロイ・環境変数設定
- [ ] 手順8: `admin/config.yml` のリポジトリ名・Worker URLを書き換え
- [ ] 手順9: 独自ドメインの接続（Claude Codeとタイミング確認の上で実施）
- [ ] 手順10: Formspreeアカウント作成・フォームID連携

途中で止まってしまっても構いません。どの番号まで進んだかを
教えていただければ、続きから一緒に進めます。
